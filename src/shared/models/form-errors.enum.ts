export enum FormErrorsEnum {
    REQUIRED = "Required",
    INVALID_CHARS = "Contains invalid characters",
    FORMAT_EMAIL = `Invalid email address`,
    LENGTH_PASSWORD = "Must contain between 8 and 64 characters",
    LENGTH_USERNAME = "Must contain between 2 and 48 characters",
}